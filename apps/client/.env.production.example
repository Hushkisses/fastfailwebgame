# 사용법: 이 파일을 복사해서 같은 폴더에 `.env.production` 이름으로 저장 후 도메인만 수정.
# Windows PowerShell 로 한 번에 빌드하려면 프로젝트 루트에서:
#   .\deploy\build-client-prod.ps1 -ApiWssUrl "wss://api.실제도메인"
#
# GAME_WEB_HOST(웹) 과 별도로, 여기는 반드시 API 서브도메인(wss) 과 일치해야 합니다.

VITE_SERVER_URL=wss://api.example.com
